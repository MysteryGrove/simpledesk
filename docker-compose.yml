version: '3.9'

services:
  web:
    build: .
    ports:
      - "8080:8000"
    env_file:
      - .env
    volumes:
      - helpdesk_data:/app/data
    restart: unless-stopped

volumes:
  helpdesk_data:
    driver: local
